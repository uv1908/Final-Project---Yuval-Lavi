export default interface Title {
    id: number;
    name: string;
    img_url: string;
    current_holder: number;
    years_active: string;
    header_url: string;
    current_holder_name?: string;
    current_holder_img?: string;
    days_held?: string;
}